import {NextRequest,NextResponse} from 'next/server';import QRCode from 'qrcode';import {requireAdmin} from '@/lib/admin-session';export async function GET(req:NextRequest){try{await requireAdmin();const slug=req.nextUrl.searchParams.get('slug');if(!slug)return NextResponse.json({error:'slug is required'},{status:400});const url=`${process.env.NEXT_PUBLIC_APP_URL||req.nextUrl.origin}/gallery/${encodeURIComponent(slug)}`;const png=await QRCode.toBuffer(url,{type:'png',width:900,margin:2,errorCorrectionLevel:'H'});return new NextResponse(new Uint8Array(png),{headers:{'content-type':'image/png','cache-control':'private, max-age=3600'}})}catch(e){return NextResponse.json({error:e instanceof Error?e.message:'QR generation failed'},{status:401})}}
