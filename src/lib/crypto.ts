import crypto from 'crypto';function key(){const raw=process.env.DRIVE_TOKEN_ENCRYPTION_KEY;if(!raw)throw new Error('DRIVE_TOKEN_ENCRYPTION_KEY is missing');const b=Buffer.from(raw,'base64');if(b.length!==32)throw new Error('DRIVE_TOKEN_ENCRYPTION_KEY must be a base64-encoded 32-byte key');return b}export function encryptText(text:string){const iv=crypto.randomBytes(12);const cipher=crypto.createCipheriv('aes-256-gcm',key(),iv);const enc=Buffer.concat([cipher.update(text,'utf8'),cipher.final()]);const tag=cipher.getAuthTag();return [iv,tag,enc].map(x=>x.toString('base64url')).join('.')}export function decryptText(payload:string){const [a,b,c]=payload.split('.');const decipher=crypto.createDecipheriv('aes-256-gcm',key(),Buffer.from(a,'base64url'));decipher.setAuthTag(Buffer.from(b,'base64url'));return Buffer.concat([decipher.update(Buffer.from(c,'base64url')),decipher.final()]).toString('utf8')}
