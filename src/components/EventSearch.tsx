"use client";import {useState} from 'react';import {useRouter} from 'next/navigation';export function EventSearch(){const [q,setQ]=useState('');const [status,setStatus]=useState('');const router=useRouter();async function go(e:React.FormEvent){e.preventDefault();setStatus('Searching…');const r=await fetch('/api/gallery/search?q='+encodeURIComponent(q));const j=await r.json();if(r.ok&&j.slug){router.push('/gallery/'+j.slug)}else setStatus(j.error||'No matching gallery found.')}return <form className="glass gold card form" onSubmit={go}><input className="input" value={q} onChange={e=>setQ(e.target.value)} placeholder="Event code, event name, bride, groom or client" required/><button className="btn btn-primary">Search Gallery</button>{status&&<p className="muted">{status}</p>}</form>}
