"use client";import {useState} from 'react';
export function BookingForm(){const [status,setStatus]=useState('');async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setStatus('Saving…');const form=new FormData(e.currentTarget);const body=Object.fromEntries(form.entries());const r=await fetch('/api/bookings',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(body)});const j=await r.json();setStatus(r.ok?'Booking request saved successfully.':j.error||'Unable to save booking.')}return <form className="glass gold card form" onSubmit={submit}><input className="input" name="name" placeholder="Name" required/><input className="input" name="mobile" placeholder="Mobile" required/><input className="input" name="whatsapp" placeholder="WhatsApp"/><input className="input" type="email" name="email" placeholder="Email"/><input className="input" name="eventType" placeholder="Event Type" required/><input className="input" type="date" name="eventDate" required/><input className="input" name="location" placeholder="Location" required/><input className="input" name="budget" placeholder="Budget (optional)"/><textarea className="input" name="message" rows={5} placeholder="Services / Message"/><button className="btn btn-primary" type="submit">Submit Booking</button>{status&&<p className="muted">{status}</p>}</form>}
