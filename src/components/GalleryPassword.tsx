"use client";import {useState} from 'react';import {useRouter} from 'next/navigation';export function GalleryPassword({eventId}:{eventId:string}){const [error,setError]=useState('');const router=useRouter();async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const password=String(new FormData(e.currentTarget).get('password')||'');const r=await fetch('/api/gallery/access',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({eventId,password})});const j=await r.json();if(!r.ok)setError(j.error||'Wrong password');else router.refresh()}return <form onSubmit={submit} className="glass gold card form" style={{maxWidth:520}}><h2>Protected Gallery</h2><input name="password" type="password" className="input" placeholder="Gallery password" required/><button className="btn btn-primary">Open Gallery</button>{error&&<p className="error">{error}</p>}</form>}
